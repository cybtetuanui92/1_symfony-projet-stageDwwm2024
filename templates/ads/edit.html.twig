{% extends 'base.html.twig' %}

{% block title %}Page ÉDITER{% endblock %}

{% block body %}
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <a class="navbar-brand" href="#">Éditer | TROC'YAH</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item active">
                    <a class="nav-link" href="{{path('app_ads_index')}}">Profil
                        <span class="sr-only">(current)</span>
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#">Éditer annonce</a>
                </li>
            </ul>
        </div>
    </nav>

    <div class="container mx-3 my-2">
        <h1>Modifiez votre annonce</h1>

        {{ include('ads/_form.html.twig', {'button_label': 'MODIFIER'}) }}

        {{ include('ads/_delete_form.html.twig') }}
    </div>
    
{% endblock %}
